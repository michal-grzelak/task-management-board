import { DefaultTheme } from 'styled-components'

import { colors } from './colors'

export const theme: DefaultTheme = {
    colors: { primary: colors.malachite, secondary: colors.persianOrange },
}
